import {StartFocusedDirective} from "./start-focused.directive";

describe("StartFocusedDirective", () => {
  it("should create an instance", () => {
    const directive = new StartFocusedDirective();
    expect(directive).toBeTruthy();
  });
});
